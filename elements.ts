import {When, For, buildInRange, always, lerp, every, sec, any, match} from './when'
Object.assign(global, {When, For, buildInRange, always, every, sec, lerp, any, match})

import './type-writer'
import './seek-able'
